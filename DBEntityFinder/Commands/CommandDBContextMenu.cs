 using Community.VisualStudio.Toolkit;
using Microsoft.VisualStudio.Shell;
using System;
using Task = System.Threading.Tasks.Task;
 

namespace DBEntityFinder
{
   // [Command("<insert guid from .vsct file>", 0x0100)]
    internal sealed class CommandDBContextMenu  //: BaseCommand<CommandDBContextMenu>
    {
        //protected override Task ExecuteAsync(OleMenuCmdEventArgs e)
        //{
        //    VS.Notifications.ShowMessage("CommandDBContextMenu", "Button clicked");

        //    return Task.CompletedTask;
        //}
    }
}
